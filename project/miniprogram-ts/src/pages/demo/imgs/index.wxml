<cm-uploader
 files="{{files}}"
 bind:change="onChange"
 types="{{types}}"
 readonly="{{readonly}}"
 title="{{title}}"
 tip="{{tip}}"
>
    <view slot="subTitle">
        副标题，这里可以自由发挥，也可以使用 subTitle 属性
    </view>
</cm-uploader>

